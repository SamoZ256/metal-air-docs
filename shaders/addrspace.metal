#include <metal_stdlib>
using namespace metal;

void test(device float* device_, constant float* constant_, thread float* thread_, threadgroup float* threadgroup_, threadgroup_imageblock float* threadgroup_imageblock_, ray_data float* ray_data_, object_data float* object_data_) { }
